using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HomingHitCheck : MonoBehaviour
{
    public bool Im_Hit = false; // This script just holds this variable that changes when it hits a resident
    //public AudioSource audioSource;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if(Im_Hit){
        //     audioSource.Play();
        // }
    }
}
